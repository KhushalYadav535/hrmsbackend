# ──────────────────────────────────────────────────────────────────────────────
# HRMS Backend — Production Environment Variables
# Copy this file to .env.production on your VPS and fill in real values.
# NEVER commit .env.production to git.
# ──────────────────────────────────────────────────────────────────────────────

# MongoDB (your VPS MongoDB — already running)
MONGODB_URI=mongodb://admin:YOUR_MONGO_PASSWORD@213.210.37.237:32768/hrms?authSource=admin

# JWT
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_STRING_MIN_64_CHARS

# Server
PORT=5000
NODE_ENV=production

# CORS — comma-separated allowed origins
CORS_ORIGIN=https://hrmssystem.vercel.app

# Redis (Bull queue) — if Redis is running on VPS
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password   # uncomment if Redis has auth
